# Sample IDs as list
samples: ../../sequencing-prep/resources/low1_sampleSheet.txt
chromosomes: ../resources/chromosome_file.txt

results_prefix: ../results
raw_read_prefix: /scratch/santang3/data 
temp_dir: /scratch/santang3/tmp 

files:
  reference_genome: /scratch/n/nessrobe/santang3/clover-ref/ref_genome/GCA_005869975.1_AgR_To_v5_genomic.fna
  gff: /scratch/n/nessrobe/santang3/clover-ref/gff/TrR.v5.renamed_reformated.gtf.gz

cluster:
  cores_per_node: 40

species_id:
  num_nt_db_files: 33  # Number of database files for NCBI's nt database (ftp://ftp.ncbi.nlm.nih.gov/blast/db/)

# Parameters for variant calling and SFS estimation with ANGSD
angsd:
  max_dp: 1250  # 2 x mean coverage from qualimap x # samples
