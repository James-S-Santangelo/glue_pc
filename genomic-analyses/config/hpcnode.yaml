# Sample IDs as list
samples: ../../sequencing-prep/resources/low1_sampleSheet.txt
chromosomes: ../resources/chromosome_file.txt

# Multiple paths to raw reads is a hotfix until reads are on Genbank
# Can be both the same if reads in in same directory (e.g., Compute Canada) 
results_prefix: ../results
glue_raw_read_prefix: /node1nfs/archive/data/trifolium/glue-low1/128.120.88.245/raw_data
tor_raw_read_prefix: /node1nfs/archive/data/trifolium/toronto_gwsd/128.120.88.251/X202SC20030702-Z01-F001/raw_data
temp_dir: /scratch/santang3/tmp 

files:
  reference_genome: /research/references/trifolium/repens/GCA_005869975.1_AgR_To_v5/GCA_005869975.1_AgR_To_v5_genomic.fna
  gff: /research/references/trifolium/repens/GCA_005869975.1_AgR_To_v5/annotation_file/TrR.v5.renamed_reformated.gtf.gz

cluster:
  cores_per_node: 30

species_id:
  num_nt_db_files: 33  # Number of database files for NCBI's nt database (ftp://ftp.ncbi.nlm.nih.gov/blast/db/)

# Parameters for variant calling and SFS estimation with ANGSD
angsd:
  max_dp: 1250  # 2 x mean coverage from qualimap x # samples

